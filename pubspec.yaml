name: issue_auto_route_incorrect_tab_height
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  auto_route: ^3.2.4
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  auto_route_generator: 3.2.3
  build_runner: 2.1.10
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true